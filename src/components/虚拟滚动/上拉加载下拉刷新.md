# 上拉加载、下拉刷新

## 上拉加载
上拉加载的本质是页面触底，或者快要触底时的动作，核心公式：
```
scrollTop + clientHeight >= scrollHeight - 阈值
```
* `scrollTop + clientHeight`：浏览器可视窗口滚动的距离 + 可视窗口的高度
* `scrollHeight - 阈值`：页面内容总高度减去一个预设的阈值，避免滚动到最底部才加载，提升体验。





## 下拉刷新

下拉刷新的本质是页面本身置于顶部时，用户下拉时需要触发的动作


